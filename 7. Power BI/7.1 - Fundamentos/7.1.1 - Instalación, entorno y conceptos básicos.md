# Power BI: instalación, entorno y conceptos básicos

**Bloque 7 -- Power BI | Sesión 1 de 21**

---

## 1. Bienvenidos al bloque de Power BI

Durante las próximas **21 sesiones** vamos a convertir Power BI en una herramienta de trabajo real. Al terminar el bloque sabremos construir un dashboard empresarial completo, desde la carga del dato crudo hasta la publicación con seguridad a nivel de fila en el servicio cloud de Microsoft. No es una exageración decir que esta es la competencia más demandada hoy en ofertas de analista de datos junior en el mercado español.

El bloque está organizado en cinco áreas:

**7.1 · Fundamentos (sesiones 1–5).** Arrancamos con el entorno, la carga de datos con Power Query, el modelado inicial, los primeros pasos en DAX y los principios de visualización. Al final de la sesión 5 tendremos nuestro primer dashboard funcional.

**7.2 · DAX y modelado intermedio (sesiones 6–11).** Aquí damos el salto cualitativo. Dejamos de "escribir medidas porque funcionan" y empezamos a entender por qué funcionan. Entran `CALCULATE`, `FILTER`, `ALL`, el análisis temporal, las variables, los iteradores, el modelado en estrella y las tablas de fechas propias.

**7.3 · Puente (sesión 12).** Power Query avanzado con parámetros y lenguaje M. Una sesión bisagra.

**7.4 · Power BI empresarial (sesiones 13–19).** Todo cambia cuando el dashboard deja de vivir en nuestro portátil: Power BI Service, gateways, seguridad a nivel de fila, rendimiento, DirectQuery, actualización incremental y pipelines de despliegue.

**7.5 · Cierre (sesiones 20–21).** Reportes paginados y proyecto final integrador.

Lo que ya traemos a este bloque es mucho. Sabemos SQL, sabemos pandas, hemos hecho análisis exploratorio con matplotlib y seaborn, hemos trabajado con procesos ETL, hemos tocado el ecosistema cloud y gobierno del dato. Power BI no va a ser un universo nuevo: va a ser una herramienta que **reutiliza todo eso** bajo una interfaz visual. Cuando escribamos una medida DAX estaremos escribiendo algo muy parecido a una función de agregación SQL. Cuando modelemos relaciones estaremos usando la misma lógica que ya usamos cuando diseñamos un esquema de base de datos. La diferencia es que aquí vamos a entregar el resultado a personas que no saben SQL, y eso cambia todo.

---

## 2. HotelesMediterraneum: el caso de estudio

Durante las 21 sesiones trabajaremos sobre una empresa ficticia pero plausible: **HotelesMediterraneum**.

### La empresa

Cadena hotelera fundada en 2008 con sede en Palma de Mallorca. Opera **40 hoteles en tres países del Mediterráneo**: España (22 hoteles, repartidos entre Baleares, Costa del Sol, Costa Brava, Canarias, y grandes ciudades como Madrid, Barcelona, Valencia, Sevilla, Bilbao y San Sebastián), Italia (12 hoteles en la Costa Amalfitana, Sicilia, Cerdeña, Roma, Venecia, Florencia y Milán) y Grecia (6 hoteles en Santorini, Mykonos, Creta, Rodas y Atenas). Segmento medio-alto, categorías de 3 a 5 estrellas, con una mezcla de hoteles vacacionales costeros y hoteles urbanos de ciudad.

El volumen de operación en los últimos tres años es de aproximadamente 1,2 millones de reservas anuales, 3 millones de noches vendidas y una base de 900.000 clientes únicos. La estacionalidad es marcada: los meses de julio y agosto concentran casi el 35% del revenue anual, con picos secundarios en Semana Santa, el puente de la Constitución y las vacaciones de Navidad en los hoteles urbanos.

### El encargo

El comité de dirección ha contratado a nuestro equipo de analítica con una petición clara y ambiciosa: **construir una plataforma de Business Intelligence en Power BI que reemplace los informes en Excel** que hoy circulan por email los lunes por la mañana. Los directores de hotel quieren ver el rendimiento de su establecimiento en tiempo cuasi-real, los directores de país quieren comparar entre hoteles, el Revenue Manager quiere entender la mezcla de canales, y el CEO quiere un panel ejecutivo con los seis KPIs principales del negocio.

### Los seis KPIs que recorrerán el bloque

Estos seis indicadores son el vocabulario oficial del sector hotelero. Los aprenderemos a calcular con DAX durante las primeras sesiones y los perfeccionaremos después.

1. **Ocupación %.** Noches vendidas dividido entre noches disponibles. Mide cuán lleno está el hotel. Un hotel con 100 habitaciones durante 30 días tiene 3.000 noches disponibles; si vende 2.400, su ocupación es del 80%.

2. **ADR — Average Daily Rate.** Ingresos totales por habitación divididos entre habitaciones vendidas. Es el precio medio por noche que está consiguiendo el hotel. Un hotel que factura 120.000 euros en 1.000 habitaciones vendidas tiene un ADR de 120 euros.

3. **RevPAR — Revenue Per Available Room.** Ingresos totales por habitación divididos entre habitaciones *disponibles* (no vendidas). Es la métrica estrella del sector porque combina precio y ocupación en un solo número. Matemáticamente equivale a `Ocupación × ADR`. Un hotel con 80% de ocupación y 120 euros de ADR tiene un RevPAR de 96 euros.

4. **Lead Time medio.** Días transcurridos entre la fecha de reserva y la fecha de check-in. Un lead time creciente indica clientes que planifican con antelación; uno decreciente señala mercado de última hora.

5. **Cancellation Rate.** Reservas canceladas divididas entre reservas totales. Crítico en el negocio post-pandemia y especialmente sensible al canal de venta.

6. **Revenue total.** Suma de ingresos, desglosable por hotel, país, canal, tipología de habitación, mes o cualquier dimensión que queramos explorar.

### Qué haremos hoy con este caso

Muy poco. Vamos a cargar el dataset de reservas, ver lo que hay dentro y construir una primera visualización que responda a la pregunta más básica del comité: **"¿cuánto facturamos el año pasado y cómo se reparte por país?"**. No porque sea la pregunta más interesante, sino porque responderla nos obliga a tocar cada pieza fundamental de Power BI al menos una vez.

---

## 3. ¿Qué es Power BI y qué no es?

Antes de instalar nada, conviene entender qué vamos a instalar. Power BI no es un único programa sino una **familia de productos** de Microsoft dedicados a la inteligencia de negocio. Los tres que nos importan son:

**Power BI Desktop.** Aplicación de escritorio para Windows (gratuita) donde se construyen los informes. Es lo que vamos a instalar hoy. Aquí se carga el dato, se modela, se escribe DAX y se diseñan las visualizaciones. Todo el trabajo de las sesiones 1 a 12 ocurre aquí.

**Power BI Service.** Servicio cloud de Microsoft donde se publican los informes para que los vean otras personas. Tiene su propia versión web y sus propias capacidades (actualización programada, seguridad a nivel de fila aplicada, apps, espacios de trabajo). Lo tocaremos desde la sesión 13 en adelante.

**Power BI Mobile.** Aplicación móvil que consume los informes publicados en el servicio. La mencionaremos pero no es foco del bloque.

Además existen **Power Query**, **DAX** y el **motor VertiPaq**, que no son productos separados sino componentes internos de Power BI Desktop. Los trataremos como si fueran herramientas distintas porque conceptualmente lo son, pero en realidad viven todos dentro del mismo .exe.

### Comparación honesta con herramientas que ya conocemos

**Power BI vs pandas.** Power BI no sustituye a pandas. Pandas es imbatible para transformación compleja, limpieza programática, machine learning y automatización. Power BI es imbatible para entregar dashboards interactivos a usuarios no técnicos que quieren filtrar, cruzar y explorar por su cuenta. En una arquitectura real, pandas suele preparar los datos aguas arriba y Power BI los consume aguas abajo. No son competidores: son eslabones distintos de la misma cadena.

**Power BI vs Excel.** Power BI es "Excel con esteroides" en la misma medida en que un coche es "un monopatín con motor": técnicamente cierto, prácticamente engañoso. Excel es imbatible para análisis ad-hoc personal y hojas pequeñas. Power BI gana en cuanto los datos superan el millón de filas, cuando necesitamos relaciones entre múltiples tablas, cuando los usuarios consumidores del informe son varios y cuando la actualización tiene que ser automática. Comparten ADN (Power Query y DAX nacieron en Excel), pero la escala y la audiencia son distintas.

**Power BI vs Tableau.** Son competidores directos en el mercado BI. Tableau brilla en libertad visual y exploración (lo veremos en el Ciclo 2). Power BI gana en integración con el ecosistema Microsoft, en modelado de datos (DAX es más potente que los cálculos de Tableau) y en precio. Ambos son herramientas de primera línea; saber Power BI hoy en España abre más puertas laborales que saber Tableau, pero saber los dos las abre todas.

### La frase que conviene grabarse

> Power BI no sustituye a Python, lo complementa. Power BI no es Excel grande, es una plataforma distinta. Power BI es la herramienta que permite que nuestro análisis lo use gente que no sabe analizar.

---

## 4. Instalación paso a paso

### Windows

Power BI Desktop es gratuito. Hay dos formas de instalarlo:

**Vía Microsoft Store (recomendada).** Abrimos la Microsoft Store, buscamos "Power BI Desktop", pulsamos instalar. Ventaja: se actualiza solo cada vez que Microsoft saca versión nueva (cadencia mensual). Desventaja: ocupa ~1,5 GB y requiere que la cuenta Microsoft del equipo tenga permisos para instalar desde la Store.

**Vía descarga directa.** Vamos a `https://www.microsoft.com/power-bi/desktop/` y descargamos el ejecutable. Ventaja: funcional en equipos corporativos donde la Store está bloqueada. Desventaja: habrá que actualizar manualmente.

Elegimos la ruta que nuestro equipo permita. Ambas instalan el mismo programa.


### macOS y Linux

Power BI Desktop **no tiene versión nativa para macOS ni Linux**. Esta es una realidad incómoda que Microsoft no ha resuelto a fecha de esta guía. Las opciones son:

1. **Máquina virtual con Windows** (Parallels, UTM, VMware Fusion). Es la opción más fiable y la recomendada para trabajar en serio con Power BI durante el curso.
2. **Boot Camp** en Macs Intel (ya no disponible en Apple Silicon).
3. **Escritorio virtual con Azure Virtual Desktop** o un proveedor tercero. Funciona bien pero tiene coste.
4. **Power BI Service únicamente.** Se puede trabajar directamente en el navegador en powerbi.com, pero las capacidades son bastante más limitadas que en Desktop. Para el bloque de clase no es suficiente.


### Primer arranque

Abrimos Power BI Desktop. Nos recibe una pantalla de bienvenida con enlaces a recursos, tutoriales y la opción de abrir un archivo reciente. La cerramos con la X de la esquina superior derecha de la pantalla de bienvenida. Queda por delante la aplicación vacía.

> [CAPTURA 4.2 — Pantalla de bienvenida de Power BI Desktop tras el primer arranque. Debe verse el título "Welcome to Power BI Desktop" o su equivalente en español, los enlaces laterales y el botón de cerrar. Encuadre completo de la ventana.]

---

## 5. Repaso consolidado de las tres vistas

Es probable que ya hayamos visto que Power BI Desktop tiene tres vistas accesibles por los tres iconos del margen izquierdo. La mayoría de usuarios principiantes las ven como "pestañas" y nunca profundizan en qué hace cada una por debajo. Vamos a arreglar eso hoy.

### Vista Report (Informe) — el icono del gráfico de barras

Es donde se **construye la visualización final** que verá el usuario. Es el lienzo donde arrastramos campos, colocamos tarjetas, gráficos y filtros, y diseñamos la presentación. Si comparamos Power BI con matplotlib: Report view es el `ax.plot(...)` + todo el trabajo de estética de la figura.

Aquí pasa el 60% del tiempo en proyectos cotidianos. Es donde un diseñador de BI invierte en tipografía, colores corporativos, alineación, interacciones entre gráficos y experiencia del usuario final.

### Vista Data (Datos) — el icono de la tabla

Muestra **las tablas cargadas en el modelo, fila a fila, como si fueran hojas de Excel**. Permite navegar, filtrar puntualmente para inspeccionar, ver tipos de datos por columna y crear columnas calculadas o tablas calculadas con DAX.

Si venimos de pandas, esta vista es nuestro `df.head()` ampliado. Lo que vemos aquí es **el resultado de la carga, después de que Power Query haya hecho su trabajo**. No es el dato crudo original; es el dato ya procesado por Power Query y residente en el motor VertiPaq en memoria.

### Vista Model (Modelo) — el icono de los tres cuadrados conectados

Es el **diagrama entidad-relación** del proyecto. Muestra todas las tablas cargadas como cajas, las relaciones entre ellas como líneas, y permite configurar propiedades del modelo (cardinalidad, dirección de filtro, tablas ocultas).

Si venimos del diseño de bases de datos relacionales, esta es nuestra vista de diagrama ER. La diferencia fundamental es que aquí **el diagrama no es solo documentación**: define cómo Power BI propaga los filtros entre tablas cuando el usuario interactúa con el dashboard. Modelar mal aquí se paga caro en las siguientes sesiones.

> ![Captura 5.1](0.Capturas\5.1_Captura.png)

### El motor que nadie ve: VertiPaq

Debajo de las tres vistas vive **VertiPaq**, el motor de Power BI. No tiene icono ni vista; es invisible. Es un motor de base de datos **columnar en memoria** que comprime los datos hasta diez veces su tamaño original y los mantiene siempre listos para consulta instantánea. Cuando arrastramos un campo al canvas y Power BI nos devuelve un gráfico con 20 millones de filas agregadas en menos de un segundo, es porque VertiPaq está haciendo magia de compresión e indexado por columna.

No necesitamos entender VertiPaq a fondo hoy. Pero sí necesitamos saber que **existe** y que es la razón por la que Power BI es rápido con volúmenes grandes donde Excel colapsaría. Volveremos al tema en la sesión 16 dedicada a rendimiento y optimización.

---

## 6. DAX vs Power Query vs visualización: los tres mundos de Power BI

Uno de los errores más frecuentes del principiante en Power BI es confundir qué herramienta usar para qué cosa. Power BI convive con **tres lenguajes y filosofías distintas** dentro de la misma aplicación. Entender dónde vive cada una nos ahorrará semanas de frustración.

### Power Query (lenguaje M)

Se usa para **transformar el dato antes de que entre al modelo**. Limpieza, tipado, renombrado, división de columnas, unión de tablas, despivotados, eliminación de duplicados. Su lenguaje se llama M y rara vez se escribe a mano; se genera con los clics de la interfaz gráfica del Editor de Power Query.

Analogía: si Power BI fuera una cocina, Power Query sería el lavado y corte de verduras antes de ponerlas en la sartén.

Todo lo que veamos en la sesión 2 vive aquí.

### Modelo de datos (vista Model)

No es un lenguaje, es una **capa de configuración**. Aquí se definen las relaciones entre tablas, la cardinalidad, la dirección de filtrado, las propiedades de cada columna (categoría de datos, formato, resumen por defecto) y la ocultación de elementos técnicos al usuario final.

Analogía: la disposición de las sartenes y las ollas en los fuegos. Decide cómo se comunican entre sí.

Las sesiones 3, 9, 10 y 11 viven aquí.

### DAX (Data Analysis Expressions)

Se usa para **calcular métricas** sobre el modelo ya cargado. Medidas como "Revenue total", "Ocupación del mismo mes del año pasado", "Ranking de hoteles por RevPAR". Es un lenguaje funcional, declarativo, parecido conceptualmente a las funciones de agregación de SQL pero mucho más potente.

Analogía: es la receta que combina los ingredientes ya preparados para crear el plato final.

Las sesiones 4, 6, 7, 8, y toda la segunda mitad del bloque conviven con DAX.

### Visualización (vista Report)

No es un lenguaje, es una **capa de presentación**. Qué gráfico elegir, qué campos arrastrar a qué parte del gráfico, qué filtros exponer al usuario, cómo se ve todo.

Analogía: emplatado y servicio.

Las sesiones 5, 20 y las de experiencia de usuario viven aquí.

### Regla mental

Cuando nos enfrentemos a un problema nuevo, nos preguntamos:

- ¿Necesitamos cambiar la forma del dato? → Power Query.
- ¿Necesitamos que dos tablas se hablen entre sí? → Modelo.
- ¿Necesitamos calcular un número que no está en el dato crudo? → DAX.
- ¿Necesitamos mostrar algo al usuario? → Visualización.

El principiante típico intenta resolverlo todo con DAX. El intermedio descubre Power Query y se pasa al otro extremo. El experto sabe en qué capa vive cada problema.

---

## 7. El dataset reservas.csv

Antes de cargar nada, es buena idea saber qué se va a cargar. El dataset de esta sesión contiene **20.000 reservas** de los años 2023 a 2025 de los 40 hoteles de HotelesMediterraneum.

### Diccionario de datos

| Columna | Tipo esperado | Descripción |
|---|---|---|
| `booking_id` | Texto | Identificador único de la reserva. Formato B100000–B119999. |
| `hotel_id` | Entero | Identificador numérico del hotel. |
| `hotel_name` | Texto | Nombre comercial del hotel. |
| `country` | Texto | País del hotel. Valores esperados: Spain, Italy, Greece. |
| `city` | Texto | Ciudad del hotel. |
| `customer_id` | Entero | Identificador del cliente. Puede repetirse (clientes recurrentes). |
| `customer_name` | Texto | Nombre completo del cliente. |
| `customer_email` | Texto | Email del cliente. |
| `channel` | Texto | Canal de venta. Valores esperados: Direct, Booking, Expedia, Agency, Corporate. |
| `room_type` | Texto | Tipología de habitación. Valores esperados: Standard, Superior, Suite, Family. |
| `meal_plan` | Texto | Régimen de comida. Valores esperados: Room Only, Breakfast, Half Board, All Inclusive. |
| `booking_date` | Fecha | Fecha en que se realizó la reserva. |
| `checkin_date` | Fecha | Fecha de entrada del cliente al hotel. |
| `checkout_date` | Fecha | Fecha de salida. |
| `nights` | Entero | Número de noches contratadas. |
| `adults` | Entero | Número de adultos. |
| `children` | Entero | Número de niños. |
| `revenue` | Decimal | Ingresos totales de la reserva en euros. |
| `status` | Texto | Estado final. Valores esperados: Confirmed, Cancelled, NoShow. |
| `is_cancelled` | Booleano | Indicador booleano de cancelación. |

Hay algo importante que notar: el grano de la tabla es **una fila por reserva**, no una fila por noche ni una fila por habitación. Una reserva de 7 noches y 2 habitaciones es una sola fila. Este es un **dato denormalizado**: información del hotel (nombre, país, ciudad) e información del cliente (nombre, email) viajan pegadas a cada reserva, en lugar de estar en tablas separadas. En la sesión 9 refactorizaremos esto a un modelo en estrella apropiado. Por ahora aceptamos la realidad: así es como nos llegan los datos en producción, y así es como los recibe el analista los lunes por la mañana.

### Advertencia sobre la calidad

Este dataset **tiene problemas intencionales de calidad**. No los vamos a resolver hoy. Los vamos a **detectar** hoy y los **resolveremos en la sesión 2** con Power Query. Esto es deliberado y pedagógico: queremos detectar los problemas nosotros mismos, antes de aprender a arreglarlos. Si en S02 partiéramos de un dataset limpio no entenderíamos para qué sirve Power Query.

---

## 8. Carga del dataset paso a paso

Con Power BI Desktop abierto y vacío, el flujo de carga es el siguiente.

### Paso 1: Obtener datos

Pulsamos **Home > Get Data** en la cinta superior. Se abre un diálogo con todos los conectores disponibles (más de 100). Buscamos "Text/CSV" o lo seleccionamos de los destacados en la parte superior del diálogo.

> [CAPTURA 8.1 — Diálogo "Get Data" de Power BI Desktop con el conector Text/CSV seleccionado o resaltado. Encuadre del diálogo completo.]

### Paso 2: Seleccionar el archivo

En el selector de archivos, navegamos hasta donde tenemos guardado `reservas.csv` y lo abrimos. Power BI abre un segundo diálogo (Navigator) que muestra una **vista previa** del contenido del archivo con las primeras filas.

> [CAPTURA 8.2 — Diálogo Navigator mostrando la vista previa de reservas.csv con las columnas visibles en la cabecera y las primeras 20 filas del contenido. Detecta si en esta captura ya se ven las fechas como texto — es útil.]

En este diálogo encontramos dos botones importantes: **Load** y **Transform Data**. La diferencia no es trivial:

- **Load** carga el archivo tal cual al modelo. Power BI adivina tipos, detecta encabezados, e incorpora la tabla al motor VertiPaq directamente. Es la ruta rápida.
- **Transform Data** abre el Editor de Power Query antes de cargar. Permite ver, inspeccionar y transformar el dato antes de que entre al modelo.

### Paso 3: Elegir Transform Data

Pulsamos **Transform Data**. Aunque hoy no vamos a transformar nada, queremos **ver** lo que tenemos. Se abre el **Editor de Power Query** en una ventana separada.

> [CAPTURA 8.3 — Editor de Power Query abierto, con la tabla reservas cargada, el panel de pasos "Applied Steps" a la derecha mostrando los pasos automáticos (Source, Promoted Headers, Changed Type), y las cabeceras de las columnas visibles con sus iconos de tipo de dato.]

### Paso 4: Observar lo que hay

Dedicamos dos minutos a **mirar** sin tocar nada. Scroll horizontal para ver todas las columnas. Scroll vertical para ver algunas filas. Lo que vamos a hacer a continuación es una **lista de diagnóstico** (sección 9), pero primero observamos con nuestros propios ojos.

Apuntamos nuestras observaciones en un bloc: ¿hay columnas raras? ¿hay valores que chirrian? ¿las fechas tienen buena pinta? Nos situamos como analistas que reciben este archivo por email y tienen que validar que está utilizable antes de montar un dashboard.

### Paso 5: Cerrar Power Query sin cambios

En la cinta de Power Query, pulsamos **Home > Close & Apply**. Esto carga el dataset al modelo y cierra el editor. Volvemos a la ventana principal de Power BI Desktop.

> [CAPTURA 8.4 — Botón "Close & Apply" resaltado en la cinta Home de Power Query Editor.]

En la vista Data ya podemos ver la tabla `reservas` cargada. En la vista Model aparece una caja con el nombre `reservas` y todas las columnas listadas.

---

## 9. Diagnóstico del dataset: qué hemos cargado

Vamos ahora a poner en práctica lo que hemos observado. Con la tabla `reservas` cargada, volvemos al Editor de Power Query (**Home > Transform Data**). Vamos a hacer un diagnóstico sistemático.

### Herramienta clave: Column Quality y Column Distribution

En la cinta del Editor de Power Query, vamos a la pestaña **View** y activamos tres casillas:

- **Column quality**: muestra para cada columna el % de valores válidos, con error y vacíos.
- **Column distribution**: muestra el número de valores distintos y únicos.
- **Column profile**: profundiza en la distribución de una columna cuando la seleccionamos.

> [CAPTURA 9.1 — Editor de Power Query con las tres opciones activadas en la pestaña View. Debe verse la tabla con las barras de calidad bajo cada cabecera de columna.]

Aparecerán unas pequeñas barras bajo cada nombre de columna: verde (válidos), rojo (errores), gris (vacíos). Junto con el número de valores distintos.

### Nuestra lista de hallazgos (spoiler parcial)

Deberíamos ser capaces de detectar estas anomalías con los ojos y con ayuda de los perfiles de columna. No hay que arreglar nada; solo **apuntarlo**. Este será el plan de trabajo de la sesión 2.

**Problemas de tipado:**

- Las columnas `booking_date`, `checkin_date` y `checkout_date` están tipadas como texto, no como fecha. Si miramos los valores veremos formatos mezclados: unas filas como `2024-06-15`, otras como `15/06/2024`, otras como `5-7-24`. Power BI no ha podido adivinar un tipo único, así que las ha dejado como texto.

**Problemas de valores:**

- La columna `channel` tiene **muchas más variantes de las que debería**. Debería haber 5 valores únicos (Direct, Booking, Expedia, Agency, Corporate) y hay 12: "Direct", "direct", "DIRECT", "Booking", "booking.com", "BOOKING", etc. Capitalización inconsistente y variaciones semánticas.
- La columna `room_type` tiene algo parecido: debería haber 4 valores y hay 8 o 9, incluyendo un typo evidente ("Stndard" en lugar de "Standard").

**Problemas de nulos:**

- La columna `meal_plan` tiene ~5% de nulos.
- La columna `customer_email` tiene ~3% de nulos.

**Problemas de coherencia entre columnas:**

- La columna `nights` no siempre cuadra con la diferencia `checkout_date - checkin_date`. Hay un pequeño porcentaje de filas inconsistentes.
- La columna `is_cancelled` (booleana) no siempre coincide con `status = "Cancelled"`. Hay redundancia entre las dos columnas y con desacuerdos entre ellas.
- La columna `children` tiene algunos valores negativos (imposibles en el negocio).

**Problemas de formato:**

- Algunos nombres de cliente tienen espacios en blanco al principio o al final ("  Maria Garcia ").

**Problemas de valores extremos:**

- `revenue` tiene unos pocos valores muy altos que parecen outliers (reservas de 10.000+ euros cuando la media está sobre 600). Podrían ser errores de captura o podrían ser reservas legítimas muy grandes. Habrá que decidir.

Esta lista es nuestro **brief para Power Query** en la sesión 2. La guardamos.

### Cerrar sin aplicar cambios

Pulsamos **Close & Apply** o simplemente cerramos Power Query descartando cambios. Para la primera visualización vamos a trabajar con el dataset "sucio" tal cual. Queremos ver qué pasa cuando Power BI trabaja con datos imperfectos.

---

## 10. Construcción del mini-dashboard

Volvemos a la ventana principal de Power BI Desktop. Abrimos la vista **Report** (icono del gráfico de barras en el margen izquierdo).

### Paso 1: Tarjeta de revenue total

En el panel **Visualizations** (derecha) buscamos el icono de **Card** (tarjeta, un número grande). Hacemos clic. Aparece una tarjeta vacía en el canvas.

En el panel **Fields** (o **Data** en versiones recientes, a la derecha del todo) expandimos la tabla `reservas` haciendo clic en la flecha. Buscamos la columna `revenue` y **la arrastramos sobre la tarjeta**.

Power BI inmediatamente muestra un número grande: algo próximo a 20 millones de euros. Acabamos de hacer nuestra primera agregación.

> [CAPTURA 10.1 — Canvas de Power BI Desktop con una Card en la esquina superior izquierda mostrando el revenue total (una cifra con muchos dígitos), y el panel Visualizations y Fields visibles a la derecha.]

### Paso 2: Formatear la tarjeta

El número aparece con muchos decimales y sin formato de moneda. Vamos a arreglarlo.

Seleccionamos la tarjeta. En el panel **Visualizations**, pasamos del primer icono (datos) al segundo (pincel, **Format**). Buscamos la sección **Callout value** y dentro, **Display units**. Seleccionamos "Millions" para que aparezca como "20 M" o similar. Añadimos también el símbolo de moneda si el campo lo permite, o lo ponemos en el título.

Cambiamos el título (también desde el panel Format) a "Revenue total 2023–2025".

### Paso 3: Gráfico de barras por país

Fuera de la tarjeta, en un área vacía del canvas, hacemos clic para deseleccionar. En el panel **Visualizations**, buscamos el icono de **Clustered bar chart** (barras agrupadas). Hacemos clic.

Aparece un gráfico vacío. Ahora:

- Arrastramos `country` al campo **Y-axis** (eje Y).
- Arrastramos `revenue` al campo **X-axis** (eje X).

Instantáneamente aparecen tres barras con los totales por país. Spain será la barra más larga (unos 12 millones), Italy la segunda, Greece la tercera.

> [CAPTURA 10.2 — Gráfico de barras por país con las tres barras visibles (Spain, Italy, Greece) y los valores de revenue en el eje horizontal. Canvas completo con la tarjeta de la izquierda también visible.]

### Paso 4: Gráfico de barras por canal

Repetimos el proceso para un tercer gráfico:

- Insertamos otro Clustered bar chart en un espacio libre del canvas.
- Arrastramos `channel` al eje Y.
- Arrastramos `revenue` al eje X.

Aquí nos llevamos una sorpresa desagradable y pedagógica: **no vemos 5 barras como esperaríamos, vemos 12**. Porque `channel` tiene 12 valores distintos gracias a todas las inconsistencias de capitalización. Habrá una barra para "Booking", otra para "booking.com", otra para "BOOKING", cada una con su porción del pastel.

Esto es exactamente el tipo de cosa que Power Query va a arreglar en la sesión 2. Por ahora, **lo aceptamos como es**. De hecho, es una consecuencia visual preciosa de la mala calidad del dato: los gráficos no mienten, y cuando el dato está sucio el gráfico está sucio.

> [CAPTURA 10.3 — Gráfico de barras por canal mostrando 10–12 barras en lugar de las 5 esperadas, evidenciando el problema de inconsistencia. Los nombres de canal con distinta capitalización deben ser legibles.]

### Paso 5: Guardar

**File > Save As**, guardamos como `HotelesMediterraneum_S01.pbix` en una carpeta de trabajo que recordemos. Lo abriremos en la sesión 2.

---

## 11. Lo que acabamos de hacer sin saberlo

Este apartado es metacognitivo: vamos a **reflexionar** sobre lo que Power BI ha hecho mientras arrastrábamos campos.

### El SQL implícito

Cuando arrastramos `revenue` a la tarjeta, Power BI ejecutó internamente algo equivalente a:

```sql
SELECT SUM(revenue) FROM reservas;
```

Cuando arrastramos `country` al eje Y y `revenue` al eje X, ejecutó:

```sql
SELECT country, SUM(revenue) AS revenue
FROM reservas
GROUP BY country
ORDER BY SUM(revenue) DESC;
```

No hemos escrito ni una línea de SQL, pero hemos producido el mismo resultado. Power BI ha **elegido la agregación por defecto** (suma, porque `revenue` es numérico), ha **elegido el GROUP BY** (implícito en el eje Y), y ha **elegido el ORDER BY** (por defecto descendente en gráficos de barras horizontales).

Esto es una bendición para prototipado rápido. Y es una trampa para análisis serio, porque **a veces Power BI adivina mal**. Cuando queramos calcular algo que no es una simple suma —una media ponderada, una diferencia entre años, un porcentaje sobre el total— Power BI no podrá adivinar. Ahí es donde entra DAX.

### La agregación automática

Al arrastrar `revenue` a un campo, aparece como "Sum of revenue". Power BI detecta que es numérico y aplica suma por defecto. Podríamos cambiarlo haciendo clic en la flecha junto al campo y eligiendo Average, Count, Min, Max, etc.

Esta agregación automática es una **columna implícita**: no existe en el modelo, se calcula al vuelo. Cuando pasemos a DAX en la sesión 4 aprenderemos a crear **medidas explícitas**, que son mucho más robustas porque se reutilizan, se formatean una vez, y se calculan de forma predecible bajo cualquier contexto de filtro.

### La respuesta parcial al comité

Hemos respondido parcialmente a la pregunta del comité: facturamos aproximadamente 20 millones de euros entre 2023 y 2025, con Spain liderando, Italy en segundo lugar y Greece en tercero. Pero muchas preguntas quedan abiertas:

- ¿Cómo evoluciona el revenue **mes a mes**? (time intelligence, sesión 7)
- ¿Cuál es el **margen real** si descontamos las cancelaciones? (DAX y filtros, sesión 6)
- ¿Qué **canal** rinde mejor de verdad, con los datos ya limpios? (Power Query, sesión 2)
- ¿Qué **hotel** es el más rentable? (modelado y jerarquías, sesiones 9–10)
- ¿Cómo entregamos esto al director general **sin que lo vean los demás directores**? (RLS, sesión 15)

Todo eso vendrá. Hoy hemos construido la base.

---

## 12. Errores comunes y troubleshooting

Los seis tropiezos típicos del primer día con Power BI:

**1. "No encontramos el botón Close & Apply."** Está en la cinta **Home** del Editor de Power Query, primera opción a la izquierda. Tiene un icono con una flecha bajando hacia una base. Si el editor está maximizado y no lo vemos, reducimos la ventana y lo veremos.

**2. "El gráfico aparece vacío aunque hemos arrastrado los campos."** Verificamos que los campos están en los slots correctos del panel **Visualizations** (parte inferior). A veces al arrastrar cae en un slot equivocado. Si hemos puesto `revenue` en un campo de "Filters" en lugar de "X-axis", el gráfico se quedará vacío.

**3. "Power BI no abre el CSV, se queda pensando."** Suele ser un archivo con encoding distinto al esperado (UTF-8). En el diálogo Navigator podemos cambiar "File Origin" a "Western European (Windows) 1252" o similar. El CSV que hemos generado usa UTF-8, así que no debería darnos este problema.

**4. "Las cifras aparecen con comas donde esperábamos puntos (o viceversa)."** Es tema de la configuración regional del equipo. Power BI respeta la configuración de Windows. No afecta a los cálculos, solo a la presentación. Se puede forzar formato fijo desde las propiedades de la columna o de la medida.

**5. "Hemos cerrado Power Query y no vemos la tabla por ningún lado."** Vamos a la vista Data (icono de tabla en el margen izquierdo). Ahí aparece la tabla cargada. Si está vacía, es que hemos pulsado Close sin Apply. Volvemos a Get Data y repetimos la carga.

**6. "Hemos tocado algo en Format y ahora la tarjeta se ve rara. ¿Cómo volvemos al estado inicial?"** Seleccionamos la visualización, y en el panel Format buscamos el botón con una flecha curvada ("Reset to default") de la propiedad que hayamos cambiado. O simplemente eliminamos la tarjeta y la creamos de nuevo.

---

## 13. Checklist de cierre y preview de S02

Antes de terminar esta sesión, deberíamos poder marcar estas ocho casillas:

- [ ] Tenemos Power BI Desktop instalado y abre sin errores.
- [ ] Sabemos distinguir las tres vistas (Report, Data, Model) y qué hace cada una.
- [ ] Entendemos qué es VertiPaq a grandes rasgos, aunque no lo hayamos tocado.
- [ ] Entendemos la diferencia entre Power Query, Modelo, DAX y Visualización.
- [ ] Hemos cargado `reservas.csv` y hemos visitado el Editor de Power Query al menos una vez.
- [ ] Hemos identificado al menos 5 problemas de calidad en el dataset.
- [ ] Hemos construido una tarjeta con revenue total y un gráfico de barras por país.
- [ ] Hemos guardado el archivo `HotelesMediterraneum_S01.pbix`.

### Qué viene en S02

En la **sesión 2 (Carga y transformación de datos — Power Query)** vamos a tomar exactamente el archivo que acabamos de guardar y, uno a uno, **vamos a resolver todos los problemas de calidad** que hemos diagnosticado hoy. Aprenderemos a estandarizar capitalización, a parsear fechas en formatos mixtos, a gestionar nulos con criterio, a detectar y tratar outliers, y a generar columnas derivadas correctas. Al final de S02 tendremos un dataset limpio y listo para modelar.

Llevamos a S02 nuestra lista de hallazgos de diagnóstico. Esa lista es el plan de la próxima clase.

---

*Guía del alumno · Sesión 1 · Bloque Power BI · HotelesMediterraneum*
